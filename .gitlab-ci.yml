image: node:latest

stages:
  - init
  - lint

build_backend:
  stage: init
  script:
    - cd backend && npm install
  artifacts:
    paths:
      - backend

build_frontend:
  stage: init
  script:
    - cd frontend && npm install
  artifacts:
    paths:
      - frontend

lint_frontend:
  stage: lint
  script:
    - cd frontend && npm install -g eslint
    - cd frontend && npm run lint
  dependencies:
    - build_frontend

